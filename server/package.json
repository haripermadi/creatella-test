{
  "name": "products-grid",
  "private": true,
  "version": "0.0.1",
  "description": "Build a grid of product items and ads",
  "main": "server/index.js",
  "scripts": {
    "start": "json-server index.js --routes routes.json --middlewares handle-delay.js handle-ads.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "",
  "dependencies": {
    "cool-ascii-faces": "^1.3.4",
    "json-server": "^0.16.1"
  }
}
